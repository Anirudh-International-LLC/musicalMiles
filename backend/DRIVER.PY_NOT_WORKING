For now, we arent using driver.py to run our services. Instead we are opting to run miles.py and refresh.py separately, while running gunicorn on port 8000 in the same computer as the frontend nginx or apache server. 
